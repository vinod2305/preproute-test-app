# Base URL for the backend API.
# Relative "/api" is proxied to the backend (Vite dev proxy + vercel.json rewrite)
# to avoid CORS. Override with a full URL only if the backend enables CORS.
VITE_API_BASE_URL=/api
